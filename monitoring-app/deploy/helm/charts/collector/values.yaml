replicaCount: 1

image:
  repository: localhost:5001/collector
  tag: dev
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 4318

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 256Mi

env: []
