apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ingestor
  labels:
    app: ingestor
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ingestor
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
