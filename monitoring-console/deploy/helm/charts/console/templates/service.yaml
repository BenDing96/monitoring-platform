apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-console
  labels:
    app: console
spec:
  type: {{ .Values.service.type }}
  selector:
    app: console
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
